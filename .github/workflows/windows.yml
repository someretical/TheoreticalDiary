on: [push, workflow_dispatch]

env:
  ACTIONS_ALLOW_UNSECURE_COMMANDS: true

jobs:
  build:
    name: Shared build
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with: 
          submodules: 'recursive'

      - name: Enable Developer Command Prompt
        uses: ilammy/msvc-dev-cmd@v1.10.0

      - name: Install Qt
        uses: jurplel/install-qt-action@v2.6.2
        with:
          host: windows
          modules: 'qtnetworkauth'
          extra: --external 7z
      
      - name: Build cryptopp
        run: |
          cd external-libs/cryptopp 
          nmake /f cryptest.nmake CXXFLAGS="/DNDEBUG /D_NDEBUG /Oi /Oy /O2 /MD"

      - name: qmake and nmake
        run: |
          qmake theoretical-diary.pro CONFIG+=release
          nmake qmake_all
          nmake

      - name: Link DLLs
        run: windeployqt release/theoretical-diary.exe --compiler-runtime

      - name: Upload to Github
        uses: actions/upload-artifact@v2
        with:
          name: theoretical-diary-x64.zip
          path: release
